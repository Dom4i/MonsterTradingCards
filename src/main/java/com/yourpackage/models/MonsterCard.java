package com.yourpackage.models;

public class MonsterCard extends Card {
    public MonsterCard(String id, String name, double damage, String elementType, String cardType) {
        super(id, name, damage, elementType, cardType);
    }
}